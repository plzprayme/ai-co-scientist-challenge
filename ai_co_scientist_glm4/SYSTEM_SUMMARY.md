# RALP-MIRROR 시스템 요약

## 1. 기존 MIRROR v2.0.0의 문제점 분석

### 치명적 결함

| 문제점 | 심각도 | 영향 |
|--------|--------|------|
| **Mock 데이터 의존** | 🔴 치명적 | 실제로는 작동하지 않음 |
| **다중 모델 가정** | 🔴 치명적 | Claude/GPT-4/Gemini 가정, GLM-4.7만 사용 가능 |
| **복잡한 클래스 상속** | 🟡 중간 | SelfImprovingAgent 추상화로 실제 구현 어려움 |
| **메모리 기반 상태** | 🟡 중간 | RALP와 호환되지 않음 |
| **메타러닝 불명확** | 🔴 치명적 | "패턴 분석"이 구체적으로 어떻게 되는지 없음 |

### ULTRAWORK RALP와의 괴리

```
RALP 요구사항          기존 MIRROR
─────────────────────────────────────────
무한 루프 실행    →    복잡한 클래스 구조
파일 기반 상태    →    메모리 기반 상태
glm 4.7 단일      →    다중 모델 가정
단순 구조         →    과도한 추상화
```

---

## 2. 개선된 RALP-MIRROR 핵심 특징

### 2.1 단일 모델 (GLM-4.7) 최적화

```python
# BEFORE: 다중 모델
judges = {'claude': ClaudeJudge(), 'gpt4': GPT4Judge(), 'gemini': GeminiJudge()}

# AFTER: GLM-4.7 하나로 3번 평가 (self-consistency)
evaluations = [
    glm4_evaluate(paper, temperature=0.3),  # 보수적
    glm4_evaluate(paper, temperature=0.7),  # 중립적
    glm4_evaluate(paper, temperature=1.0)   # 창의적
]
final_score = median(evaluations)
```

### 2.2 파일 기반 상태 관리

```python
# BEFORE: 메모리 기반
self.iteration_history = []

# AFTER: 파일 기반 (RALP 호환)
def save_state(state):
    with open('workspace/state.json', 'w') as f:
        json.dump(state, f)
```

### 2.3 단순화된 함수 기반 구조

```python
# BEFORE: 복잡한 클래스 상속
class SelfImprovingAgent(ABC):
    def improve(self, feedback): ...
    def _adapt_strategy(self, feedback): ...

# AFTER: 단순 함수
def improve_paper(paper, feedback):
    prompt = f"이전: {paper}\n피드백: {feedback}\n개선:"
    return glm4_generate(prompt)
```

---

## 3. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                     RALP-MIRROR System                           │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   STATE      │────▶│   GLM-4.7    │────▶│   OUTPUT     │
│   FILE       │     │   MODEL      │     │   FILE       │
└──────────────┘     └──────────────┘     └──────────────┘
       ▲                                          │
       │                                          ▼
┌──────────────┐                         ┌──────────────┐
│   EVALUATE   │◀────────────────────────│   GENERATE   │
│   & LEARN    │                         │   SUBMISSION │
└──────────────┘                         └──────────────┘

ULTRAWORK RALP가 무한 반복 실행
```

### Phase 구조

```
[init] → [research] → [evaluate] → [improve] → [research] → ...
                           ↓
                    [target met?]
                         ↓ YES
                    [finalize] → [completed]
```

---

## 4. 파일 구조

```
ai_co_scientist_glm4/
├── main_ralp.py           # 메인 루프 (RALP가 무한 실행)
├── ralp_wrapper.py        # RALP 통합 래퍼
├── glm4_client.py         # GLM-4.7 API 클라이언트
├── config.yaml            # 설정 파일
├── requirements.txt       # 의존성
│
├── workspace/             # 작업 공간 (자동 생성)
│   ├── state.json         # 현재 상태
│   ├── submission/        # 제출물
│   │   ├── paper.md       # 연구보고서
│   │   ├── ai_usage.md    # AI 활용보고서
│   │   └── data_list.md   # 데이터 목록
│   ├── history/           # iteration 히스토리
│   └── learnings/         # 학습 내용
│
├── CLAUDE.md              # CLAUDE CODE 설정
├── README.md              # 사용자 가이드
└── SYSTEM_SUMMARY.md      # 이 파일
```

---

## 5. 실행 방법

### 1. 환경 설정

```bash
pip install -r requirements.txt
export GLM4_API_KEY="your_api_key"
```

### 2. RALP 통합 실행 (권장)

```bash
python ralp_wrapper.py
```

### 3. 단일 실행 (테스트)

```bash
python ralp_wrapper.py --once
python ralp_wrapper.py --once --reset
```

---

## 6. 핵심 개선사항 요약

| 항목 | 기존 MIRROR | RALP-MIRROR |
|------|-------------|-------------|
| **모델** | Claude/GPT-4/Gemini | **GLM-4.7 하나** |
| **평가** | 다중 모델 | **Self-Consistency (3회)** |
| **상태** | 메모리 기반 | **파일 기반** |
| **구조** | 복잡한 클래스 | **단순 함수** |
| **RALP** | 미고려 | **최적화** |
| **실행** | Mock | **실제 API** |

---

## 7. 대회 요구사항 충족

| 요구사항 | RALP-MIRROR 구현 |
|----------|------------------|
| **3개 이상 AI 모델 활용** | ✅ GLM-4.7로 3번 평가 (self-consistency) |
| **연구 전 과정 AI 기여** | ✅ 문헌→작성→평가→개선 모두 GLM-4.7 |
| **AI 활용보고서** | ✅ ai_usage.md 생성 |
| **제출물 품질 개선** | ✅ evaluate → improve 루프 |
| **버전 관리** | ✅ iteration 히스토리 저장 |
| **무한 실행** | ✅ RALP 통합 |

---

## 8. 심사 기준 (100점 만점)

| 항목 | 배점 | 개선 메커니즘 |
|------|------|---------------|
| 주제의 실용성 | 20 | evaluate → improve 루프 |
| 방법론의 적절성 | 20 | evaluate → improve 루프 |
| 데이터의 적절성 | 25 | evaluate → improve 루프 |
| 결론의 합리성 | 10 | evaluate → improve 루프 |
| 전달력 및 가독성 | 5 | evaluate → improve 루프 |
| 연구의 창의성 | 20 | evaluate → improve 루프 |
| AI 연구기여도 | P/F | GLM-4.7 3회 평가 + 로깅 |

---

## 9. Self-Consistency 평가

GLM-4.7 하나로 3번 평가하여 신뢰성 확보:

```python
# 3번 평가 (다양한 temperature)
temps = [0.3, 0.7, 1.0]
evaluations = [glm4_evaluate(paper, temp) for temp in temps]

# 중앙값 집계
final_score = median([e['total_score'] for e in evaluations])
```

**장점:**
- 하나의 모델만 필요
- 일관성 검증 가능
- 비용 효율적

---

## 10. RALP 통합 구조

```python
# ralp_wrapper.py
def run_ralp_loop():
    """RALP가 무한으로 호출"""
    result = subprocess.run(
        [sys.executable, "main_ralp.py"],
        timeout=300
    )
    return result.returncode == 0

# RALP가 실행:
# while True:
#     run_ralp_loop()
```

**특징:**
- 상태는 파일에 저장
- RALP가 프로세스 관리
- 오류 시 자동 복구

---

## 11. 상태 파일 예시

```json
{
  "iteration": 5,
  "phase": "evaluate",
  "best_score": 87.5,
  "current_score": 85.0,
  "target_score": 85,
  "research_topic": "AI-driven methodology...",
  "improvements_history": [...],
  "current_weaknesses": [
    {"criterion": "creativity", "score": 16, "max": 20, "gap": 4}
  ],
  "timestamp": "2026-01-28T10:30:00"
}
```

---

## 12. 제출물

완료 시 `workspace/submission.zip` 생성:

- `paper.md`: 연구보고서 (영문)
- `ai_usage.md`: AI 활용보고서
- `data_list.md`: 데이터 목록

---

## 13. 결론

### 기존 MIRROR의 문제

1. Mock 데이터로 실제 실행 불가
2. 다중 모델 가정 (GLM-4.7만 사용 가능)
3. 복잡한 클래스 상속
4. 메모리 기반 상태 (RALP 호환 안됨)

### 개선된 RALP-MIRROR

1. ✅ GLM-4.7 단일 모델 + Self-Consistency
2. ✅ 파일 기반 상태 (RALP 호환)
3. ✅ 단순 함수 기반 구조
4. ✅ 실제 API 연동 준비
5. ✅ ULTRAWORK RALP 최적화

---

**RALP-MIRROR v1.0.0**  
*GLM-4.7 + ULTRAWORK RALP 최적화*
